paramiko>=3.0.0
scp>=0.14.5
requests>=2.31.0
tqdm>=4.66.0
python-dateutil>=2.8.2
click>=8.1.0
pathlib2>=2.3.7
cryptography>=41.0.0
PyPDF2>=3.0.0
svglib>=1.5.0
reportlab>=4.0.0
keyring>=24.0.0
rmc>=0.3.0

# OCR and image handling
pdf2image>=1.16.0
Pillow>=10.0.0
pystray>=0.19.5

# AI providers (optional – install the ones you need)
# pip install anthropic       (Claude)
# pip install openai          (GitHub Models / OpenAI)
# pip install pytesseract     (offline OCR fallback; also needs system Tesseract)
